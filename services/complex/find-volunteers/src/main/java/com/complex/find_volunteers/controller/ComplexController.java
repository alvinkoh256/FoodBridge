package com.complex.find_volunteers.controller;

import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.RestController;

@RestController
@CrossOrigin(origins = "*")
public class ComplexController{
    // Any required services
    public ComplexController(){
        // declare as we go
    }

    // @PostMapping("/findVolunteers")
    // public ResponseEntity<String> findVolunteers(@RequestParam("file")MultipartFile inputFile, @RequestParam())
}